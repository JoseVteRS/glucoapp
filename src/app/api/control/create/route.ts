


export async function POST(request: Request) {
    // const session = await getServerSession(authOptions)
    // const controls = await prisma.control.create({
    //     data: {

    //     }
    // })
    // return NextResponse.json(controls)
    throw new Error("Not implemented");
}